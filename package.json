{
  "name": "@vectojs/numera-skills",
  "version": "0.1.0",
  "description": "Agent Skills for safe local Numera workbook automation.",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "bun test",
    "format:check": "prettier --check .",
    "lint": "oxlint --deny-warnings test",
    "verify": "bun run format:check && bun run lint && bun test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/bun": "^1.3.0",
    "oxlint": "^1.74.0",
    "prettier": "^3.9.1"
  }
}
