# Changesets

Add one Changeset for every user-visible skill behavior change. This repository
is distributed through GitHub; Changesets still record version intent while
npm publication remains disabled.
